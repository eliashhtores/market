from applications.product.models import Product


def update_stock(self, instance, **kwargs):
    # product = Product.objects.get(pk=self.product.pk)
    # product.qty_available -= self.quantity
    # product.save()
    pass
